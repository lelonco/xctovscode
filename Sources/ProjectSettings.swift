
import Foundation

struct ProjectSettings {
    /// Workspace -showBuildSettings
    // BUILD_DIR =  /Users/yaroslav/Library/Developer/Xcode/DerivedData/Test-heteydoaltxowafixegsrcamyxii/Build/Products
    // CONFIGURATION = Test Debug // Be aware that this string may have spaces need to be escaped
    // CONTENTS_FOLDER_PATH = Test Application.app // Looks like i can use it for app name. Be aware that this string may have spaces need to be escaped.
    // CORRESPONDING_SIMULATOR_PLATFORM_NAME = iphonesimulator // Current xcode platform selected
    // CORRESPONDING_SIMULATOR_SDK_NAME = iphonesimulator17.2 // Current xcode sdk selected
    // PRODUCT_BUNDLE_IDENTIFIER = com.yaroslav.test // Bundle id

    /// Project -showBuildSettings
    //
}
